# ===========================================
# CoPiloto Mobile (React Native) - Environment
# ===========================================
# Copy this file to .env and fill in your values
#
# To find your DEV_IP:
#   Windows: ipconfig → IPv4 Address
#   macOS/Linux: ifconfig → inet

# Development IP (for local testing)
EXPO_PUBLIC_DEV_IP=192.168.x.x

# Production Backend URL
EXPO_PUBLIC_PROD_URL=http://your-server-ip:4000

# Google Maps API Key
# IMPORTANT: Restrict this key in Google Cloud Console by:
#   - Android: Add your app's SHA-1 fingerprint + bundle ID
#   - iOS: Add your bundle ID
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# SECURITY NOTES:
# - All variables starting with EXPO_PUBLIC_ are included in the JavaScript bundle
# - They are readable by anyone who decompiles your APK/IPA
# - Never put real passwords, private keys, or secrets here
# - Guest/demo credentials are hardcoded in LoginScreen.tsx (public by design)
