# ===========================================
# CoPiloto Mobile (React Native) - Environment
# ===========================================
# This file documents the configuration needed.
# Update the API_URL in src/api/client.ts with your local network IP.
#
# To find your IP:
#   Windows: ipconfig → IPv4 Address
#   macOS/Linux: ifconfig → inet

# Backend API URL (update in src/api/client.ts)
API_URL=http://YOUR_LOCAL_IP:4000/api
